{
  "name": "widget",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config vite.contact-form.config.ts && vite build --config vite.returns-portal.config.ts && vite build --config vite.review-widget.config.ts && vite build --config vite.review-badge.config.ts && vite build --config vite.tracking-widget.config.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
